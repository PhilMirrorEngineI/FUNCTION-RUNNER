openai>=1.40,<2
flask>=3.0,<4
gunicorn>=21.2,<22
requests>=2.31,<3
reportlab>=4.2,<5
# (pillow only if you manipulate images after generation)
# pillow>=10.3,<11
# python-dotenv>=1.0,<2 (only for local .env)
